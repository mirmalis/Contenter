@inherits _Creator<Contenter.Models.Contents.ContentFam>
@rendermode InteractiveServer
@code {
  //[Parameter]
  //public Guid? XId { get; set; }
  [Parameter]
  public Guid? ParentId { get; set; }
  override protected async Task OnParametersSetAsync()
  {
    await base.OnParametersSetAsync();
    //if (this.XId != null)
    //{
    //}
    if (this.ParentId != null)
    {
      this.model.Parent = await this.db.Set<Contenter.Models.Contents.ContentFam>().FirstOrDefaultAsync(item => item.Id == this.ParentId);
    }
  }
}
<_Bind model=@this.model OnValidSubmitCallback=@(async (model) => await this.DoCreate(this.model))>
  @ChildContent
</_Bind>
