@code {
  [EditorRequired]
  [Parameter]
  public ISourceViewInner? Model { get; set; }

  [Parameter]
  public string? Name { get; set; }
}

@if (this.Model != null)
{
  <AnchorBase href=@($"/s/sources/{this.Model.Id}")
              Text=@(this.Name ?? this.Model.Name) />
}
